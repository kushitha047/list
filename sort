***
Write a program to sort the given list and print it.
Sample Input:
30 20 10 50 40
Sample Output:
10 20 30 40 50 
***
# Read the list elements from the input line
elements = list(map(int, input().split()))

# Sort the list
sorted_elements = sorted(elements)

# Print the sorted list elements space-separated followed by "program"
print(" ".join(map(str, sorted_elements)), "program")
